<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#1A1A2E">

    <!-- App Icon / Emoji -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="💡"
        android:textSize="72sp"
        android:layout_marginBottom="16dp" />

    <!-- App Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Life Hack or Urban Myth?"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textColor="#E94560"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Description -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Think you can tell the difference between a genuine life hack and a viral urban myth? Test your common sense with our flashcard quiz!"
        android:textSize="15sp"
        android:textColor="#CCCCCC"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="40dp" />

    <!-- Start Button -->
    <Button
        android:id="@+id/btnStart"
        android:layout_width="200dp"
        android:layout_height="56dp"
        android:text="START QUIZ"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:backgroundTint="#E94560"
        android:layout_marginBottom="24dp" />

    <!-- Footer note -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="10 Questions · Learn Real-World Tips"
        android:textSize="12sp"
        android:textColor="#888888" />

</LinearLayout>